{
  "name": "code-interpreter-app",
  "version": "1.0.0",
  "description": "Full-featured code interpreter web application",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run prisma:generate --workspace=backend && npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:production": "NODE_ENV=production npm run build",
    "start": "concurrently \"npm run start --workspace=frontend\" \"npm run start --workspace=backend\"",
    "start:frontend": "npm run start --workspace=frontend",
    "start:backend": "npm run start --workspace=backend",
    "type-check": "npm run type-check --workspace=shared && npm run type-check --workspace=backend && npm run type-check --workspace=frontend",
    "lint": "npm run lint --workspace=shared && npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "test:unit": "npm run test --workspace=shared && npm run test --workspace=backend && npm run test --workspace=frontend",
    "clean": "npm run clean --workspace=shared && npm run clean --workspace=backend && npm run clean --workspace=frontend"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "concurrently": "^8.2.2",
    "eslint-plugin-react": "^7.35.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "typescript": "5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
