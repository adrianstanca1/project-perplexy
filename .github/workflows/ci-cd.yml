name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mongodb:
        image: mongo:7.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: |
          cd packages/backend && npm ci
          cd ../frontend && npm ci
      
      - name: Run backend tests
        run: |
          cd packages/backend
          npm run test
        env:
          DATABASE_URL: mongodb://localhost:27017/constructai-test
      
      - name: Run frontend tests
        run: |
          cd packages/frontend
          npm run test
      
      - name: Lint
        run: |
          cd packages/backend && npm run lint
          cd ../frontend && npm run lint

  build:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Build backend
        run: |
          cd packages/backend
          npm ci
          npm run build
      
      - name: Build frontend
        run: |
          cd packages/frontend
          npm ci
          npm run build
      
      - name: Docker build
        run: |
          docker-compose -f docker-compose.prod.yml build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to production
        run: |
          echo "Deployment steps would go here"
          # Add your deployment commands (AWS, GCP, Azure, etc.)

